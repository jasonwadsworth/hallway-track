## Remove a contact link
#set($userId = $ctx.identity.sub)

## First, get the current user to find the contact link
{
  "version": "2018-05-29",
  "operation": "GetItem",
  "key": {
    "PK": $util.dynamodb.toDynamoDBJson("USER#$userId"),
    "SK": $util.dynamodb.toDynamoDBJson("PROFILE")
  }
}
